<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- No push service at all: Doze suspends the connection Jami needs to
         receive calls, so the exemption is the only way to stay reachable. -->
    <bool name="battery_optimization_exemption_requestable">true</bool>

</resources>
